# Full dependency set: runtime plus the test toolchain. The App Store python
# template test stage runs `pip install -r requirements.txt` then `pytest`, so
# the test tools must live here. The Docker IMAGE installs requirements-runtime.txt
# instead, keeping test tooling (and its CVEs) out of the deployed container.
#
# sgp4 is a deliberate, recorded runtime dependency: the validated Vallado SGP4
# propagator for sub-satellite longitude.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
certifi==2026.6.17
click==8.4.2
fastapi==0.139.2
gunicorn==26.0.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
packaging==26.2
pydantic==2.13.4
pydantic_core==2.46.4
sgp4==2.27
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
pytest==9.0.3
pytest-cov==7.0.0
